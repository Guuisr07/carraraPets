import React from 'react'
import styled from 'styled-components/native'

interface SuccessSignUpScreen {}

const StyledContainer = styled.View`
  flex: 1;
  background-color: white;

`

export const SuccessSignUpScreen: React.FC<any> = ({}) => {
  return (
    <StyledContainer>
      
    </StyledContainer>
  )
}